# Kkongdon Sample Site

This is the sample static site for the personal-brand-site agent.

Edit:

- `index.html` for copy and structure
- `styles.css` for visual style
- `script.js` for interaction

Replace `https://example.com/` after deployment.

